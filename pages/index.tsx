import React from "react";
import {Button} from "react-bootstrap";

const Home = () => {
    return (
        <div>
            <Button variant={"primary"}>ああ</Button>
        </div>
    )
}

export default Home